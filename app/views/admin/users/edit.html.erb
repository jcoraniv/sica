<%= render Ui::CardComponent.new(title: t("admin.users.edit.title"), subtitle: t("admin.users.edit.subtitle")) do %>
  <%= render "form", user: @user, url: admin_user_path(@user), method: :patch, submit_text: t("admin.shared.actions.save_changes"), back_path: admin_users_path, show_admin_fields: true, zones: @zones, categories: @categories %>
<% end %>

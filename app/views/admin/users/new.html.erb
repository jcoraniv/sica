<%= render Ui::CardComponent.new(title: t("admin.users.new.title"), subtitle: t("admin.users.new.subtitle")) do %>
  <%= render "form", user: @new_user, url: admin_users_path, method: :post, submit_text: t("admin.users.new.submit"), back_path: admin_users_path, show_admin_fields: true, zones: @zones, categories: @categories %>
<% end %>
